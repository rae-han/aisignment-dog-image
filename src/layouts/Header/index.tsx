import React from 'react';
import {HeaderContainer} from "@/layouts/Header/styles";

const Header = () => {
  return (
    <HeaderContainer>
      <header className="Header">
        <h1 className="Header__title">Woof.tv</h1>
      </header>
    </HeaderContainer>
  );
};

export default Header;